# VMMS 数据库整理报告

**整理日期**: 2026-05-05  
**数据库**: MySQL 8.0 / vmms  
**项目**: 车辆维修管理系统 (VMM)

---

## 一、数据库表结构分析

### 1.1 表总览（29张表 → 整理后27张表）

| 分类 | 表数量 | 表名 |
|------|--------|------|
| 核心业务 | 5 | `customer`, `vehicle`, `orders`, `bustatus`, `paystatus` |
| 用户权限 | 5 | `userinfo`, `account`, `sector`, `permission`, `userrights` |
| 配件库存 | 8 | `part`, `partcategory`, `supplier`, `warehouse`, `partstorage`, `partproc`, `partused`, `accessoryhis` |
| 维修业务 | 8 | `projcategory`, `mainitem`, `mainprojreg`, `qualityinspec`, `personallocate`, `maintenance_progress`, `fault_type`, `fault_symptom` |
| 财务报表 | 3 | `balancesheet`, ~~`arrears`~~, `consumsettle`, ~~`materhis`~~ |
| 客服日志 | 5 | `customervisithis`, `notification`, `service_evaluation`, `vehicle_maintenance_history`, `operation_log` |

### 1.2 核心表关联关系

```
customer  1───*  vehicle
customer  1───*  orders
vehicle   1───*  orders
orders    1───*  mainprojreg / partused / personallocate / balancesheet
orders    1───*  maintenance_progress / notification / service_evaluation
sector    1───*  userinfo
userinfo  1───*  account
account   1───*  userrights *───1  permission
partcategory 1──* part *───1 supplier
part      1───*  partstorage *───1 warehouse
part      1───*  partused 1───* accessoryhis
projcategory 1──* mainitem 1───* mainprojreg 1───* qualityinspec
```

**核心表**: `orders`（工单）是业务中心，关联客户、车辆、维修、配件、财务全链路。

---

## 二、已删除的无用表

### 2.1 `arrears`（欠款表）- 已删除

| 项目 | 说明 |
|------|------|
| **删除原因** | 仅有 Mapper 定义，无任何 Service/Controller 层调用 |
| **替代方案** | 欠款功能可通过 `balancesheet` 表的结算状态字段实现 |
| **已删除文件** | `ArrearsMapper.java`, `Arrears.java`, `ArrearsMapper.xml` |
| **已清理引用** | `DaoFactory.java`（字段+getter/setter）, `generatorConfig.xml` |

### 2.2 `materhis`（材料消耗历史表）- 已删除

| 项目 | 说明 |
|------|------|
| **删除原因** | 仅有 Mapper 定义，无任何 Service/Controller 层调用 |
| **替代方案** | 材料消耗记录功能已由 `partused` + `accessoryhis` 表完整覆盖 |
| **已删除文件** | `MaterhisMapper.java`, `Materhis.java`, `MaterhisMapper.xml` |
| **已清理引用** | `DaoFactory.java`（字段+getter/setter）, `generatorConfig.xml` |

### 2.3 `vehicle_maintenance_history`（车辆维修历史表）- 保留

最初分析认为该表无业务调用，但进一步检查发现：
- `VehicleInfoController.java` 中有 `/getVehicleMaintenanceHistory` 接口
- `vehicleInfoMana.jsp` 前端页面有调用该接口的按钮
- **结论：该表为活跃使用的功能，已保留**

---

## 三、测试数据清理情况

### 3.1 硬编码初始化数据（保留）

| 表 | 数据 | 说明 |
|----|------|------|
| `bustatus` | 4条（待处理/处理中/已完成/已提车） | 系统必要基础状态数据 |
| `paystatus` | 2条（未支付/已支付） | 系统必要基础状态数据 |

这两组数据在 `VehicleMaintence.java` 中通过 `initDefaultBustatus()` 和 `initDefaultPaystatus()` 自动初始化，是系统正常运行必需的，**已保留**。

### 3.2 测试数据情况

- 项目中未发现 `.sql` 初始化脚本或测试数据插入代码
- 测试类 `DatabaseConnectionTest.java` 仅做数据库连接测试，未插入数据
- 数据库中的数据需通过 SQL 脚本手动检查清理（见 `database_cleanup.sql`）

---

## 四、文件变更清单

### 4.1 已删除文件（6个）

| 文件路径 | 类型 |
|----------|------|
| `src/main/java/com/javaweb/dao/ArrearsMapper.java` | DAO 接口 |
| `src/main/java/com/javaweb/entity/Arrears.java` | Entity 类 |
| `src/main/resources/mapping/ArrearsMapper.xml` | MyBatis 映射 |
| `src/main/java/com/javaweb/dao/MaterhisMapper.java` | DAO 接口 |
| `src/main/java/com/javaweb/entity/Materhis.java` | Entity 类 |
| `src/main/resources/mapping/MaterhisMapper.xml` | MyBatis 映射 |

### 4.2 已修改文件（2个）

| 文件 | 修改内容 |
|------|----------|
| `src/main/java/com/javaweb/dao/DaoFactory.java` | 移除 ArrearsMapper 和 MaterhisMapper 的字段、getter/setter |
| `src/main/java/com/javaweb/views/generatorConfig.xml` | 移除 arrears 和 materhis 表配置 |

### 4.3 新增文件（2个）

| 文件 | 用途 |
|------|------|
| `database_cleanup.sql` | 数据库清理 SQL 脚本（删除表 + 优化建议） |
| `数据库整理报告.md` | 本报告 |

---

## 五、数据库优化建议

### 5.1 索引优化

以下字段在高频查询中使用但缺少索引，建议添加：

```sql
-- 订单表（核心查询频繁按客户、车辆、状态过滤）
ALTER TABLE orders ADD INDEX idx_customerid (customerid);
ALTER TABLE orders ADD INDEX idx_vehicleid (vehicleid);
ALTER TABLE orders ADD INDEX idx_bustatusid (bustatusid);

-- 车辆表
ALTER TABLE vehicle ADD INDEX idx_customerid (customerid);

-- 配件使用表
ALTER TABLE partused ADD INDEX idx_ordersid (ordersid);

-- 维修项目登记表
ALTER TABLE mainprojreg ADD INDEX idx_ordersid (ordersid);
```

### 5.2 表结构优化

| 建议 | 说明 |
|------|------|
| `account.passwords` 改为哈希存储 | 当前明文存储密码，存在安全隐患 |
| `orders` 表增加 `create_time`/`update_time` | 便于数据追溯和审计 |
| `consumsettle` 与 `balancesheet` 考虑合并 | 两者功能有重叠，均记录结算信息 |

### 5.3 数据维护

```sql
-- 定期清理30天前的操作日志
DELETE FROM operation_log WHERE operation_time < DATE_SUB(NOW(), INTERVAL 30 DAY);

-- 定期优化大表
OPTIMIZE TABLE orders, customer, vehicle, part, partused, mainprojreg;
```

---

## 六、执行清单

- [x] 代码层：删除 arrears/materhis 相关文件（6个）
- [x] 代码层：清理 DaoFactory 和 generatorConfig 引用（2个文件）
- [ ] 数据库层：执行 `database_cleanup.sql` 中的 DROP TABLE 语句
- [ ] 数据库层：备份数据库后执行索引优化（可选）
- [ ] 应用层：重新编译部署验证系统功能正常
